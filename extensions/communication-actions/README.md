# Communication Actions

This first-party package detects bounded email addresses and phone numbers offline. Its actions use manifest-declared `compose_email` and `dial_phone` handlers; the guest never receives a generic URI or shell capability.

Build the WASM component, copy it to `component.wasm`, then package and validate it with the ClipsX extension tool.
