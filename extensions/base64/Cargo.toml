[package]
name = "clipsx-base64"
version = "1.8.7"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.57.1"

[profile.release]
opt-level = "s"
lto = true
strip = true
