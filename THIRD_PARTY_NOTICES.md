# Third-party notices

First-party ClipsX extensions use the packages recorded in `package-lock.json`.
Those packages retain their respective licenses and copyright notices. Run
`npm run license:check` before publishing an extension release.

`khroma` 2.1.0 omits the SPDX field from its package metadata but includes an
MIT license file in the distributed package; the policy records that reviewed exception.
